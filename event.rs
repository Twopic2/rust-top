/* use std::{io, time::Duration, Write};
use crossterm::event::{self, Event, KeyCode, KeyEventKind};

pub enum MainEvent {
    KeyInput(KeyEvent),
    MouseInput(MouseEvent),
}

#[derive(Debug)]
pub struct full_event {
    TICK_RATE: Duration,
    time: u64,
} */